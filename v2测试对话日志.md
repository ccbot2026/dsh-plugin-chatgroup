# V2 测试对话日志

> 项目：dsh-plugin-chatgroup v2.0.0（分支 feat/dsh-plugin-chatgroup-v2）
> 环境：http://127.0.0.1:3081/（3081 实例，worktree 加载 v2 构建）
> 测试者：sll
> 开始日期：2026-08-16

---

## 测试目标

按 `docs/plan.md` V2 规划逐项验证：多群 / 消息编辑撤回 / AI 主动发言 / 首轮中断摘要 / 面板增强。

## 测试准备

- 已确认 main 保持 v0.2.0 基线（未合并），V2 仅在 feat 分支，测试期间可放心改动。
- 已修改 `DEFAULT_MAX_GROUPS = 2`（默认允许 2 个群，便于测试多群），构建并重启 3081。
- 面板配置表单补充了 `maxGroups` 输入框（原缺失）。

## 测试记录

### AI 群聊反馈核实（2026-08-16，来源：`/home/sll/projects/v2测试反馈.md`）

群聊 AI（ds-a/ds-b）反馈了若干问题，经逐条核实：

| AI 反馈 | 核实结果 | 处理 |
|---|---|---|
| "src/dist 漂移，src 是单群、dist 是多群，npm test 会抹掉多群" | ❌ **不属实**。v2 worktree 的 src 就是多群实现；AI 读的是 main worktree（v0.2.0 基线，因用户要求未合并） | 无需处理；已向用户说明根因 |
| "test/ 下没有 multigroup.test.mjs" | ❌ **不属实**。v2/test/multigroup.test.mjs 存在（6 用例） | 无需处理 |
| "V2.3 配置键 dist 有 src 无" | ❌ **不属实**。v2/src 全部实现存在 | 无需处理 |
| "V2.4 摘要串了上个话题，缺 topicId 过滤" | ✅ **属实（真 bug）** | **已修复**：`appendFirstRoundInterruptSummary` 增加 `topicId === currentTopicId` 过滤（commit b387c16） |
| "mentionIds 从未填充（AI 消息恒空）" | ⚠️ **部分属实**。AI 消息 mentionIds 恒空属实；用户 @ 场景会填充 | **已修复**：`resolveAiMentions` 文本匹配后回填 mentionIds（commit b387c16） |

**核实方法**：对比 v2 worktree（`dsh-plugin-chatgroup-v2/`）与 main worktree（`dsh-plugin-chatgroup/`）的 src；查群聊 session 的 `.dsh/chatgroup/` 落盘位置（在 main worktree 下，cwd 继承所致）确认 AI 读取目录。

**关键教训**：群聊 AI 成员的工作目录继承自群聊会话的 cwd（本例落在 main worktree），它们读的 src 与实际运行的 v2 dist 不同源——AI 的代码审查结论可能基于错误目录，必须人工核实。

### 已修复

- `b387c16`：V2.4 摘要 topic 限定 + AI mentionIds 回填；新增跨话题回归测试；52/52 全绿。
- `f7e65b2`：V2.6 成员 token usage 显示（输入/输出/缓存命中）；53/53 全绿。
- 面板重构：设置侧栏 4 分区折叠 + 配置项中文标签/当前值/说明（commit 前），已构建上线。

### 已完成验证

- [x] **V2.4 首轮中断摘要**：topicId 修复后重测通过（用户确认"摘要正常了"）。
- [x] **群清理**：group-1 解散（日志已删）；group-2 残留已再解散；`.dsh/chatgroup` 下 dbaed85f 日志清理。
- [x] **AI 互 @ 链路**（早期 ds-a/ds-b 验证）：#23 追加回应 ✓、#24 深度拦截 ✓、#25 防死循环 ✓（基于旧 dist，修复后需在干净群复验）。
- [x] **mentionIds 回填**：已修复并在 usage/互@ 测试中覆盖。

### 待办 / 进行中

- [ ] V2.1 多群：建群、切换、隔离、持久化、v1 迁移（干净群复验）
- [ ] V2.2 消息编辑/撤回：编辑、撤回、AI 上下文影响、窗口限制
- [ ] V2.3 AI 主动发言 + AI 互 @：开关、上限、深度限制、防循环（干净群复验）
- [ ] V2.5 群重命名、工具活动提示
- [ ] V2.6 成员 usage 显示（刚上线，待人工验证）
- [ ] 回归：自动多轮 / 新议题 / @ / 盲发 / 复制导出

### 发现的问题

#### P1 — 配置面板需要重新组织（用户反馈 2026-08-16）

现状问题：

1. **功能混杂**：群名称、开启新议题、自动多轮讨论、群配置、成员管理、@ 开关全部堆在同一个设置侧栏里，没有分区，视觉上难以定位。
2. **placeholder 无法表达当前值**：配置输入框有值时 placeholder 不显示，用户看不到"当前配置是什么"；且 placeholder 直接用配置 key（如 `maxAi`、`defaultTimeoutMs`），没有中文说明，可读性差。
3. **空间不足**：所有配置挤在一个固定宽度（320px）侧栏里，行数多、信息密度低，无法承载更多说明文字。

修复方案（已确认方向）：

- 配置面板按功能**分区折叠**（群信息 / 讨论控制 / 群配置 / 成员管理），用 `<details>` 折叠节省空间。
- 每个配置项改为**中文标签 + 当前值 + 说明**的完整行，不再依赖 placeholder 传达含义。
- 状态：进行中（面板重构后需重新构建 + 重启 3081 验证）。

## 备注

- 测试期间的代码改动直接提交到 `feat/dsh-plugin-chatgroup-v2` 分支。
- 测试全部通过并确认后，再决定是否合并回 main。
