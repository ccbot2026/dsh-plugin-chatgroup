# V2 测试对话日志

> 项目：dsh-plugin-chatgroup v2.0.0（分支 feat/dsh-plugin-chatgroup-v2）
> 环境：http://127.0.0.1:3081/（3081 实例，worktree 加载 v2 构建）
> 测试者：sll
> 开始日期：2026-08-16

---

## 测试目标

按 `docs/plan.md` V2 规划逐项验证：多群 / 消息编辑撤回 / AI 主动发言 / 首轮中断摘要 / 面板增强。

## 测试准备

- 已确认 main 保持 v0.2.0 基线（未合并），V2 仅在 feat 分支，测试期间可放心改动。
- 已修改 `DEFAULT_MAX_GROUPS = 2`（默认允许 2 个群，便于测试多群），构建并重启 3081。
- 面板配置表单补充了 `maxGroups` 输入框（原缺失）。

## 测试记录

### 待办 / 进行中

- [ ] V2.1 多群：建群、切换、隔离、持久化、v1 迁移
- [ ] V2.2 消息编辑/撤回：编辑、撤回、AI 上下文影响、窗口限制
- [ ] V2.3 AI 主动发言 + AI 互 @：开关、上限、深度限制、防循环
- [ ] V2.4 首轮中断摘要
- [ ] V2.5 群重命名、工具活动提示
- [ ] 回归：自动多轮 / 新议题 / @ / 盲发 / 复制导出

### 发现的问题

#### P1 — 配置面板需要重新组织（用户反馈 2026-08-16）

现状问题：

1. **功能混杂**：群名称、开启新议题、自动多轮讨论、群配置、成员管理、@ 开关全部堆在同一个设置侧栏里，没有分区，视觉上难以定位。
2. **placeholder 无法表达当前值**：配置输入框有值时 placeholder 不显示，用户看不到"当前配置是什么"；且 placeholder 直接用配置 key（如 `maxAi`、`defaultTimeoutMs`），没有中文说明，可读性差。
3. **空间不足**：所有配置挤在一个固定宽度（320px）侧栏里，行数多、信息密度低，无法承载更多说明文字。

修复方案（已确认方向）：

- 配置面板按功能**分区折叠**（群信息 / 讨论控制 / 群配置 / 成员管理），用 `<details>` 折叠节省空间。
- 每个配置项改为**中文标签 + 当前值 + 说明**的完整行，不再依赖 placeholder 传达含义。
- 状态：进行中（面板重构后需重新构建 + 重启 3081 验证）。

## 备注

- 测试期间的代码改动直接提交到 `feat/dsh-plugin-chatgroup-v2` 分支。
- 测试全部通过并确认后，再决定是否合并回 main。
