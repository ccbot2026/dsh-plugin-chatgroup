# chat-group E2E 检查清单

> 适用版本：v0.4.1（含 V2 多群/编辑撤回/主动发言/摘要 + 0.4.x 面板）

## 准备

```sh
cd /home/sll/projects/dsh-plugin-chatgroup
npm test
npm pack
dsh plugin --profile web add /path/to/dsh-plugin-chatgroup-0.4.1.tgz
dsh web --port 3081
```

浏览器打开 `http://127.0.0.1:3081/`，选择一个项目会话。

## 手工用例

| # | 步骤 | 预期 |
|---|---|---|
| 1 | 会话浮动按钮打开群聊 | 抽屉打开（左右两栏：消息 + 设置） |
| 2 | 创建群聊，添加 2-3 个 AI，设置顺序 | 成员和顺序正确显示 |
| 3 | 发送议题 | AI 按顺序发言，流式上屏 |
| 4 | 第一轮查看 prompt/观察 | 首轮盲发；用户始终可见 |
| 5 | 第二轮发言 | 全部成员看到第一轮历史 |
| 6 | 运行中插话 | 后续 AI 可见，顺序不变 |
| 7 | @ 某个 AI | 仅目标 AI 单独回应；AI 消息 mentionIds 回填 |
| 8 | 结束本轮 | 当前发言取消，群 idle |
| 9 | 拖拽面板宽度并刷新 | 宽度保持 |
| 10 | `/group auto 3 议题` | 连续 3 轮，系统消息轮次提示 |
| 11 | 自动讨论中 `/group stop` | 自动模式终止 |
| 12 | 重启 dsh | 群从 `.dsh/chatgroup/*.jsonl` 恢复 |
| 13 | 切换会话 | 面板跟随新会话 |
| 14 | 移除成员/解散群 | 状态与持久化正确 |
| 15 | 消息超过 100 条 | 顶部显示计数，滚动加载更早消息 |

### V2 多群（v0.3.0）

| # | 步骤 | 预期 |
|---|---|---|
| 16 | `/group config` | 显示全部 11 项配置（含 maxGroups/maxEditableMessages/aiProactive 等） |
| 17 | 面板把 maxGroups 改为 2 → 保存 → `/group create` | 能建 group-2；再建报"最多 2 个群" |
| 18 | `/group list`、`/group use group-1` | 列表正确，切换生效 |
| 19 | 两群各加不同成员、各发消息 | 群间完全隔离 |
| 20 | 建 2 群各发言 → 重启 → 打开 | 两群都从各自 jsonl 恢复 |
| 21 | 群选择器下拉 + 新建群按钮 | 面板顶部切换/新建正常 |

### V2 编辑/撤回（v0.3.0）

| # | 步骤 | 预期 |
|---|---|---|
| 22 | 消息气泡 hover → 编辑 → 保存 | 显示"已编辑"标注 + 新文本 |
| 23 | `/group edit <seq> <text>` | 同 22 |
| 24 | 气泡 → 撤回 | 变"（已撤回）" |
| 25 | 撤回后新一轮 | AI 看不到撤回内容 |
| 26 | 发 30+ 条，编辑最早的 | 报"只能编辑最近 20 条" |

### V2 AI 主动/互 @（v0.3.0）

| # | 步骤 | 预期 |
|---|---|---|
| 27 | 不配 aiProactive 跑一轮 | 轮末 AI 不主动说话 |
| 28 | 面板开 aiProactive → 跑一轮 | 轮末每 AI 被问一次，补充 ≤ 上限；"无需补充"被识别 |
| 29 | AI 回复写 `@成员名` | 被 @ 者追加回应一次（深度 1） |
| 30 | 链式 @ | 第二跳拦截 + "深度已达上限"系统提示，无死循环 |

### V2.4/V0.4.1 首轮中断摘要

| # | 步骤 | 预期 |
|---|---|---|
| 31 | 首轮发言中点"结束本轮"（有人已发言、有人未发言） | 出现**真摘要**（AI 概括，非截断原文） |
| 32 | 正常跑完不中断 | 无摘要消息 |

### V2.5 面板

| # | 步骤 | 预期 |
|---|---|---|
| 33 | 群管理区"重命名" | 面板标题/list/status 显示新名，重启保持 |
| 34 | 群管理区"导出群聊" | 下载 Markdown |
| 35 | AI 发言中观察 | 面板顶部显示"成员 X 正在 read …" |

### 0.4.x 面板 UI

| # | 步骤 | 预期 |
|---|---|---|
| 36 | 拖拽设置面板左缘 | 宽度变化并记忆 |
| 37 | header 折叠图标 | 设置面板收起/展开 |
| 38 | 点击群面板外部 | 面板自动隐藏 |
| 39 | 输入框聚焦 | 外层胶囊边框高亮（圆角统一） |
| 40 | 成员列表 | 显示 `输入 X · 输出 Y · 缓存 Z%`（发言后） |

## API 冒烟

```sh
# 创建 session、群、成员、自动讨论、快照
# 详见 docs/smoke-test.md
# RPC 全链路（多群/编辑撤回/wait 隔离）由 test/rpc-e2e.test.mjs 覆盖
```
